# NextAuth configuration
# App URL (e.g., http://localhost:3000 for local dev)
NEXTAUTH_URL=http://localhost:3000
# Use `openssl rand -base64 32` to generate a strong secret
NEXTAUTH_SECRET=changeme-please

# Backend base URL for credentials authorize() to call
# e.g., http://localhost:5000 or your API gateway base URL
AUTH_BACKEND_URL=http://localhost:5000
